# Workspace-specific themes
# Workspace 2: Minimal But Warm

# Give workspace 2 a name and minimal styling
workspace = 2, name:warm, gapsout:8, gapsin:4, bordersize:1, rounding:true

# Workspace 2 specific window rules - cleaner look
# windowrulev2 = bordersize 1, workspace:2
# windowrulev2 = rounding 14, workspace:2
