servers = (
  {
    address = "irc.libera.chat";
    chatnet = "liberachat";
    port = "6697";
    use_tls = "yes";
    tls_verify = "yes";
    autoconnect = "yes";
    tls_cert = "/home/aarav/.config/irssi/certs/libera.pem";
  },
  {
    address = "irc.ionic1k.org";
    chatnet = "ionic1k";
    port = "6697";
    use_tls = "yes";
    autoconnect = "yes";
  }
);

chatnets = {
  liberachat = {
    type = "IRC";
    max_kicks = "1";
    max_msgs = "4";
    max_whois = "1";
    sasl_mechanism = "EXTERNAL";
    sasl_username = "areofyl";
    sasl_password = "";
  };
  ionic1k = {
    type = "IRC";
    max_kicks = "1";
    max_msgs = "4";
    max_whois = "1";
  };
};

channels = (
  { name = "#gentoo"; chatnet = "liberachat"; autojoin = "yes"; },
  { name = "#asahi"; chatnet = "liberachat"; autojoin = "yes"; },
  { name = "#c"; chatnet = "liberachat"; autojoin = "yes"; },
  { name = "#anime"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#general"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#tech"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#techsupport"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#unix"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#networking"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#radio"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#pol"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#vctxt"; chatnet = "ionic1k"; autojoin = "yes"; },
  { name = "#bridge"; chatnet = "ionic1k"; autojoin = "yes"; }
);

aliases = {
  J = "JOIN";
  K = "KICK";
  KB = "KICKBAN";
  LEAVE = "PART";
  M = "MSG";
  N = "NAMES";
  Q = "QUERY";
  T = "TOPIC";
  WC = "WINDOW CLOSE";
  WG = "WINDOW GOTO";
  WI = "WHOIS";
  WII = "WHOIS $0 $0";
  WN = "WINDOW NEW HIDDEN";
  SB = "SCROLLBACK";
  C = "CLEAR";
  EXIT = "QUIT";
  BYE = "QUIT";
};

statusbar = {
  items = {
    barstart = "{sbstart}";
    barend = "{sbend}";
    topicbarstart = "{topicsbstart}";
    topicbarend = "{topicsbend}";
    time = "{sb $Z}";
    user = "{sb {sbnickmode $cumode}$N{sbmode $usermode}{sbaway $A}}";
    window = "{sb $winref:$tag/$itemname{sbmode $M}}";
    window_empty = "{sb $winref{sbservertag $tag}}";
    prompt = "{prompt $[.15]itemname}";
    prompt_empty = "{prompt $winname}";
    topic = " $topic";
    topic_empty = " Irssi v$J - https://irssi.org";
    lag = "{sb Lag: $0-}";
    act = "{sb Act: $0-}";
    more = "-- more --";
  };
  default = {
    window = {
      disabled = "no";
      type = "window";
      placement = "bottom";
      position = "1";
      visible = "active";
      items = {
        barstart = { priority = "100"; };
        time = { };
        user = { };
        window = { };
        window_empty = { };
        lag = { priority = "-1"; };
        act = { priority = "10"; };
        more = { priority = "-1"; alignment = "right"; };
        barend = { priority = "100"; alignment = "right"; };
      };
    };
    window_inact = {
      type = "window";
      placement = "bottom";
      position = "1";
      visible = "inactive";
      items = {
        barstart = { priority = "100"; };
        window = { };
        window_empty = { };
        more = { priority = "-1"; alignment = "right"; };
        barend = { priority = "100"; alignment = "right"; };
      };
    };
    prompt = {
      type = "root";
      placement = "bottom";
      position = "100";
      visible = "always";
      items = {
        prompt = { priority = "-1"; };
        prompt_empty = { priority = "-1"; };
        input = { priority = "10"; };
      };
    };
    topic = {
      type = "root";
      placement = "top";
      position = "1";
      visible = "always";
      disabled = "yes";
      items = {
        topicbarstart = { priority = "100"; };
        topic = { };
        topic_empty = { };
        topicbarend = { priority = "100"; alignment = "right"; };
      };
    };
  };
};

settings = {
  core = { 
    real_name = "areofyl";
    user_name = "areofyl";
    nick = "areofyl";
  };
  "irc/core" = { skip_motd = "yes"; };
  "fe-common/core" = {
    activity_hide_level = "JOINS PARTS QUITS NICKS";
    beep_msg_level = "HILIGHT";
    hilight_act_color = "%M";
    theme = "minimalist";
  };
};
ignores = ( { level = "JOINS PARTS QUITS NICKS"; } );
